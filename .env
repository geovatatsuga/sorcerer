## Local environment for development
# Update the DATABASE_URL credentials to match your local Postgres setup if needed.
# Example for default local Postgres (change password if yours is different):
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/sorcerer

# Session secret for express-session
SESSION_SECRET=dev_secret_change_me

# Optional: change PORT if you want
# PORT=5000
# Local development environment variables
# Update USER:PASSWORD if your local Postgres uses different credentials
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/sorcerer
PORT=5000
SESSION_SECRET=dev_secret_change_me
